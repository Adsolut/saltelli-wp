# v1.0.0 Wave 1 COMPLETE — ACF Field Groups Setup

> Recovery Wave 1 sequenziale single-agent (post-fail dei 2 agent paralleli per saturazione sistema).
> Aggiornato: 2026-05-04. Branch: `feat/wave1-agent-b-cpt-fields`.

---

## 🎯 Score: 16/16 Field Groups · 142 fields totali

```
Agent A (5 page WP)         5  ✓  recovery (questa sessione)
Agent B (10 CPT)           10  ✓  pre-recovery (commit f1c1051)
Agent C (1 Theme Options)   1  ✓  recovery (file recuperato da inc/acf-json/)
                          ───
                           16  ✓
```

---

## 📦 Per phase

### Phase 1 — Agent A retry (5 Field Groups page WP)

| Key | Title | Fields | Page (slug → ID) | Commit |
|---|---|---|---|---|
| `group_costi_v1` | Costi — Sezioni | 16 | `costi` → 2695 | f7d0639 |
| `group_casi_v1` | Casi — Sezioni | 10 | `casi` → 2699 | fcee78d |
| `group_contatti_v1` | Contatti — Sezioni | 10 | `contatti` → 23 | 4294e46 |
| `group_faq_v1` | FAQ Aggregator — Sezioni | 10 | `faq` → 2705 | 342da48 |
| `group_info_shared_v1` | Info Shared — Layout standard | 16 | OR (5 page) | 736f265 |

**info_shared OR clauses**: guide-gratuite (2706), come-lavoriamo (2709), prima-consulenza (2708), lavora-con-noi (372), richiedi-preventivo (2710).

Commit incrementale per Field Group (atomicity).

### Phase 2 — Agent C retry (1 Field Group Theme Options)

| Key | Title | Fields | Tabs | Commit |
|---|---|---|---|---|
| `group_theme_options_v1` | Saltelli — Settings globali | 32 (26 + 6 tab) | Studio Info · Mappa · Brand · Footer · Social · CTA Defaults | 836ea30 |

**Discovery**: il file `group_theme_options_v1.json` (18.7KB, completo per spec) era già stato generato in `inc/acf-json/` da un precedente Agent C ma non committato. Recovery: spostato a `acf-json/` (path canonico Wave 1) + commit.

### Phase 3 — Cleanup Wave 0 placeholders (commit b166fa2)

Rimossi 3 placeholder Wave 0 in `inc/acf-json/`, superati dai `_v1` Wave 1:

| File rimosso | Key | Motivo |
|---|---|---|
| `group_avvocato.json` | `group_saltelli_avvocato` | Repeater requires ACF Pro (mai installato); duplica `group_avvocato_v1` |
| `group_competenza.json` | `group_saltelli_competenza` | Repeater requires ACF Pro; duplica `group_competenza_v1` |
| `group_settings.json` | `group_saltelli_settings` | Repeater requires ACF Pro; **DUPLICATE** `options_page=saltelli-settings` con `group_theme_options_v1` (avrebbe creato 2 metabox sullo stesso settings page) |

Inoltre semplificato `inc/acf-fields.php`:
- Rimossi `acf/settings/load_json` + `save_json` filter custom (puntavano a `inc/acf-json/`).
- ACF Free 6.8.0 ora usa il path di default `wp-content/themes/saltelli/acf-json/` (root) — singola location, no duplicati.
- Mantenuto `acf_add_options_page()` (supportato da ACF Free).

---

## 🗂 Files su disk

```
wp-content/themes/saltelli/acf-json/
├── group_avvocato_v1.json              # Agent B
├── group_casi_v1.json                  # Agent A.2 (recovery)
├── group_caso_item_v1.json             # Agent B
├── group_competenza_v1.json            # Agent B
├── group_contatti_v1.json              # Agent A.3 (recovery)
├── group_costi_v1.json                 # Agent A.1 (recovery)
├── group_faq_item_v1.json              # Agent B
├── group_faq_v1.json                   # Agent A.4 (recovery)
├── group_formazione_item_v1.json       # Agent B
├── group_guida_item_v1.json            # Agent B
├── group_info_shared_v1.json           # Agent A.5 (recovery)
├── group_modalita_item_v1.json         # Agent B
├── group_principio_item_v1.json        # Agent B
├── group_scenario_item_v1.json         # Agent B
├── group_theme_options_v1.json         # Agent C (recovery, file pre-esistente recuperato)
└── group_trust_item_v1.json            # Agent B
```

`inc/acf-json/` directory eliminata (era 100% placeholder).

---

## 📊 Smoke verify

```
ACF Free 6.8.0:                  active
acf_add_local_field_group:       OK
Load paths configured:           1 (default acf-json/)
Field groups registered:         16 (atteso 16) ✓
Field groups duplicate keys:     0 (cleanup placeholder ok) ✓
Pages target esistenti:          9/9 (costi/casi/contatti/faq + 5 info pages) ✓

Frontend smoke locale (HTTP 200):
  /                  200  /casi/             200
  /chi-siamo/        200  /costi/            200
  /avvocati/         200  /contatti/         200
  /faq/              200  /come-lavoriamo/   200

Theme Version:  1.0.0-recovery-wave0 → 1.0.0-recovery-wave1
```

---

## 🔄 Branch & deploy state

**Branch**: `feat/wave1-agent-b-cpt-fields` (NON `main` come scritto nel recovery prompt)
- 7 commit recovery (Agent A × 5 + Agent C + cleanup) sopra il commit Agent B (f1c1051)
- Target merge: `main` via PR alla fine della Wave 1
- **Push**: locale only finora — no push a origin

**Droplet staging** (`/var/www/saltelli/`):
- Stato attuale: pre-recovery (solo Wave 0 + Agent B su staging via rsync ad-hoc)
- **NON ancora deployato**: i 5 Agent A + 1 Agent C non sono su staging
- Action richiesta: rsync acf-json/ + style.css + functions.php + cache flush
- Auto mode: skipped, attende conferma user

---

## 🚦 Next: Wave 2 — Content Migration (~2-3h)

Schema pronto. Ora popolare i ~142 fields con valori da hardcoded template PHP.

```
Wave 2 task list:
  ☐ /costi/                       16 fields hero+aside+§03 body+CTA
  ☐ /casi/                        10 fields + N CPT saltelli_caso items
  ☐ /contatti/                    10 fields hero+map+come+trust
  ☐ /faq/                         10 fields + 28+ CPT saltelli_faq items + topic taxonomy
  ☐ /guide-gratuite/              16 fields (info_shared) + N CPT saltelli_guida
  ☐ /come-lavoriamo/              16 fields (info_shared)
  ☐ /prima-consulenza/            16 fields (info_shared)
  ☐ /lavora-con-noi/              16 fields (info_shared)
  ☐ /richiedi-preventivo/         16 fields (info_shared)
  ☐ 4 avvocato                    12 fields × 4 = 48 fields
  ☐ 19 competenza (3 tier-1+16)   11 fields × 19 = 209 fields (focus tier-1)
  ☐ 3 modalita CPT                4 × 3 = 12 fields
  ☐ 3 scenario CPT                4 × 3 = 12 fields
  ☐ 4 trust CPT                   2 × 4 = 8 fields
  ☐ 3 principio CPT               3 × 3 = 9 fields
  ☐ N formazione CPT              3 × N
  ☐ Theme Options                 26 fields (NAP, brand, footer, social, CTA)
```

Strategia Wave 2: parsing hardcoded da `page.php` (1274 righe) → WP-CLI `wp post meta update` per populating ACF fields.

---

## ⚠️ Note utenti / handoff

1. **Cliente NON vede ancora il CMS popolato** in WP-Admin: i field group esistono, le pagine target esistono, ma i valori non sono ancora migrate dal template PHP. Wave 2 risolverà.
2. **Frontend invariato**: tutti i template PHP hanno fallback hardcoded via helper `saltelli_field()`. Visualmente identico finché Wave 2 non popola.
3. **WP-Admin nuovo**: aprendo una page costi/casi/contatti/faq, vedrai la sidebar metabox ACF con i nuovi fields. Tutti vuoti per ora.
4. **Lessons applied**:
   - Sequenziale single-agent (load avg 13 → no MCP timeout)
   - Smoke test post ogni Field Group (zero regressioni)
   - Commit incrementale (atomicity, rollback chirurgico)
   - Path canonicalization (acf-json/ root unico, no inc/acf-json/ dual)

---

*Generated 2026-05-04 by Claude Code recovery agent.*
*Cumulative Wave 1 commit: f1c1051 → b166fa2 + bump version (8 commit Wave 1).*
