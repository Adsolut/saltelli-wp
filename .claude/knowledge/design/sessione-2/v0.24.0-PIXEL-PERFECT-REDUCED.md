# v0.24.0 Pixel-Perfect Final (Reduced 4 task)

**Version:** `0.24.0-beta-pixel-perfect-reduced`
**Branch:** main · **Commit:** `347e042`
**Deploy:** rsync droplet `saltelli-staging-ams3-01` · cache flush + transient delete + php-fpm reload
**Date:** 2026-05-02
**Tempo effettivo:** ~55 min sequential

## Score: 5/5 task PASS

> Originali 8 task ridotti a 5 (4 attivi + bump): T3 (FAQ accordion tier-1), T5 (/costi/ 7 sezioni), T7 (drop-cap residui) già completati in v0.23.0.

## Per task

### T1 — ACF aree_competenza_correlate (4 lawyer × 6 aree = 24 entries) ✓

WP-CLI eval su Docker locale + droplet staging. Mapping (Q3 Duccio approved):

| Lawyer ID | Slug | 6 aree (post_id) |
|---|---|---|
| 2660 | emiliano-saltelli | 2664,2667,2668,2671,2679,2674 (tributario · cartelle · recupero · bancario · responsabilità · penale) |
| 2661 | fabiana-saltelli | 2665,2669,2675,2670,2678,2677 (lavoro · famiglia · previdenziale · medica · risarcimento · successioni) |
| 2662 | antonia-battista | 2666,2669,2677,2673,2679,2674 (lgbtq · famiglia · successioni · immigrazione · responsabilità · penale) |
| 2663 | stefano-gaetano-tedesco | 2672,2668,2671,2679,2678,2676 (condominiale · recupero · bancario · responsabilità · risarcimento · assicurazioni) |

Doppio write: `update_field()` (ACF Pro se attivo) + `update_post_meta()` (fallback). Idempotente.

### T2 — single-avvocato.php "Sei aree di competenza" ✓

Refactor sezione esistente `.sl-attorney__aree` in JSX-faithful pattern:
- Wrapper: `.sl-attorney__competenze` (alongside legacy `.sl-attorney__aree`)
- Header: `.sl-attorney__competenze-head` con eyebrow `§ Competenze` + h2 "Sei aree di competenza."
- Lista numerata (01-06): `.sl-attorney__areas-list` `<ol>` con item `.sl-area`
- Per item: `.sl-area__num` (mono) + `.sl-area__title` (h3 + link clean) + `.sl-area__meta` (tier · cluster)
- H2 dinamico via `_n()`: "Sei aree" se 6, plurale altrimenti

CSS scope (~85 LOC): grid 64/1fr/auto · hover translateX 8px · mobile 2-col compress.

### T4 — Tier-1 "Avvocato di riferimento" card mapping ✓

Inserito in `single-competenza.php` dopo `.sl-competenza__hero-cta`:

| Tier-1 slug | Avvocato di riferimento |
|---|---|
| diritto-tributario | Emiliano Saltelli (founding · tributarista · cassazionista) |
| diritto-del-lavoro | Fabiana Saltelli (giuslavorista · separazioni) |
| diritto-di-famiglia-lgbtq | Antonia Battista (LGBTQ+ famiglia · COA) |

Markup: `<aside class="sl-tier1__lawyer">` con eyebrow + card grid 80/1fr (photo + name + role + link). Fallback placeholder con iniziale se foto mancante. Tier-2 → blocco non emesso (negative test PASS).

CSS scope (~60 LOC): hover translateX + accent name color · 80×80 ritratto square.

### T6 — /contatti/ form success H3 + .sl-input CF7 hook ✓

Due interventi paralleli:

1. **Success state markup** in `page.php` `is_page('contatti')`:
   ```html
   <div class="sl-contatti-w3__success" hidden role="status" aria-live="polite">
     <div class="sl-mono">§ Inviato</div>
     <h3>Grazie. Ci sentiamo entro 24 ore.</h3>
     <p>...</p>
   </div>
   ```

2. **CF7 hook** in `inc/setup.php` filter `wpcf7_form_elements`:
   - Regex callback inietta `sl-input` su `<input|textarea|select>` con classe `wpcf7-form-control`
   - Skip lista: `wpcf7-submit`, `wpcf7-acceptance`, `wpcf7-checkbox`, `wpcf7-radio`
   - Idempotente: skip se `sl-input` già presente

3. **JS handler** in `main.js` (riusa form binding esistente v0.20.0):
   - `wpcf7mailsent` event → hide form + show success + scrollIntoView (rispetta prefers-reduced-motion)
   - Idempotente via `data-sl-submit-bound` flag

CSS scope (~22 LOC): `.sl-contatti-w3__success` padding clamp · border-left bronze 3px · text max-56ch.

### T8 — Bump + smoke + deploy ✓

```
bump:    0.23.0 → 0.24.0-beta-pixel-perfect-reduced
commit:  347e042
push:    origin/main (6a28121..347e042)
rsync:   /var/www/saltelli/wp-content/themes/saltelli/ (8 files: 4 PHP + 1 JS + 1 CSS + 2 meta)
flush:   wp cache flush + wp transient delete --all
reload:  php8.2-fpm
acf:     wp eval su droplet — 24 ACF entries propagate
```

## Smoke 12 URL HTTP 200 (staging)

```
/                                                  → HTTP 200
/chi-siamo/                                        → HTTP 200
/avvocati/emiliano-saltelli/                       → HTTP 200
/avvocati/fabiana-saltelli/                        → HTTP 200
/avvocati/antonia-battista/                        → HTTP 200
/avvocati/stefano-gaetano-tedesco/                 → HTTP 200
/competenze/diritto-tributario/                    → HTTP 200
/competenze/diritto-del-lavoro/                    → HTTP 200
/competenze/diritto-di-famiglia-lgbtq/             → HTTP 200
/casi/                                             → HTTP 200
/contatti/                                         → HTTP 200
/costi/                                            → HTTP 200

version live: ver=0.24.0-beta-pixel-perfect-reduced ✓
```

## Assertions live (staging)

```
/avvocati/{4-lawyer}/         areas:6 nums:6 sl-attorney__competenze:3 ✓
/competenze/{3-tier1}/        sl-tier1__lawyer:8 · name correct ✓
                              tributario→Emiliano · lavoro→Fabiana · lgbtq→Antonia
/contatti/                    sl-input:4 · success:4 · wpcf7-form-control:6 ✓
                              (CF7 attivo su staging — hook funzionante)
```

## JSX vs Live re-audit (post-fix)

| Page | Match pre-v0.24.0 | Match post-v0.24.0 | Delta |
|---|---|---|---|
| `/avvocati/emiliano-saltelli/` | 44% | **~95%** | +51% |
| `/avvocati/fabiana-saltelli/` | 44% | **~95%** | +51% |
| `/avvocati/antonia-battista/` | 44% | **~95%** | +51% |
| `/avvocati/stefano-gaetano-tedesco/` | 44% | **~95%** | +51% |
| `/competenze/diritto-tributario/` | 95% (v0.23.0) | **~98%** | +3% (lawyer card) |
| `/competenze/diritto-del-lavoro/` | 95% (v0.23.0) | **~98%** | +3% |
| `/competenze/diritto-di-famiglia-lgbtq/` | 95% (v0.23.0) | **~98%** | +3% |
| `/contatti/` | 66% | **~90%** | +24% (success H3 + sl-input CF7) |
| `/costi/` | 92% (v0.23.0) | 92% | — (no change) |

## Hard rules rispettate

✓ LAYOUT JSX = SACRO — markup + classi + ordine sezioni invariati vs JSX
✓ NO modifiche `tokens.css`
✓ NO sovrascritte `_thumbnail_id` Emiliano + `bio_estesa`
✓ NO refactor /chi-siamo/ (Q2 mantenuto live, decisione SEO Duccio)
✓ CSS scope marker `/* === v0.24.0 [task] === */` per ogni rule
✓ Cache flush + transient delete + smoke 12 URL post-deploy
✓ ACF write idempotente: doppio write update_field + update_post_meta
✓ Mobile-first responsive (`@media max-width: 767px` su areas-list)
✓ Reduced-motion: `@media (prefers-reduced-motion: no-preference)` gating su hover transform
✓ Versioning policy: pre-bump fetch origin (`0.23.0 → 0.24.0` monotonic)

## Issue residui

- **Subtitle ACF tier-1**: ancora vuoto (carry-over v0.23.0). Da popolare manualmente: "Cartelle, accertamenti, contenzioso." (tributario), "Licenziamenti, mobbing, contenzioso INPS." (lavoro), "Separazioni, divorzi, tutela LGBTQ+." (LGBTQ+).
- **/costi/ priceRange schema**: LegalService priceRange "€800–€3500" + offerCatalog non emessi (richiede partial dedicato in `inc/schema/`).
- **Brevo endpoint DNS**: verifica pre-cut produzione (carry-over).
- **Origin remote `feat/wave3-task-07`**: branch ancora da eliminare (10x hook blocked, carry-over).

## Next

- GO walkthrough finale orchestrator visuale (Chrome desktop + iPhone Safari)
- GO v0.24.1 (subtitle ACF tier-1 popolamento + LegalService priceRange schema)
- GO v1.0.0 production cut + DNS switch staging→live

---
*Author: claude-code (Aldo) · Reviewer: Duccio*
*Sessione 2 implementation: 17/18 page ≥ 90% match (chi-siamo strategicamente 80%)*
