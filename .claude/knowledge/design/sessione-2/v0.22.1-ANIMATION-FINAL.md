# v0.22.1 + v0.22.2 Animation Consistency Final

**Versions:**
- `0.22.1-beta-animation-final` (commit `751f3df`) — first round
- `0.22.2-beta-animation-final-completion` (commit `17a3fbb`) — Duccio audit residui carry-over

**Branch:** main · **Live:** `0.22.2-beta-animation-final-completion`
**Date:** 2026-05-02

## Score: 3/3 task PASS

## Per task

### A — Hero stagger esteso archive/taxonomy/glossario/blog ✓ (v0.22.1)
Helper `saltelli_split_h1_words()` applicato a 4 template:

| File | h1 class | Output |
|---|---|---|
| `archive-avvocato.php` | `.sl-section-title` | "Quattro<br>professionisti." → 2 sl-word |
| `home.php` | `.sl-blog2__h1` | "Editoriale." → 1 sl-word |
| `taxonomy-tipo-area.php` | `.sl-tipoarea__h1` | dinamico (term + breadcrumb) |
| `inc/wave3-glossario.php` | `.sl-glossario__h1` | "Glossario<br>legale." → 2 sl-word |

**Helper v0.22.2:** ora accetta optional `$extra_class` param (backward-compatible) per modifier classes (es. `'sl-section-title__word--em'`).

**CSS:** scope esteso `.sl-section-title/.sl-blog2__h1/.sl-tipoarea__h1/.sl-glossario__h1 .sl-word` + transition-delay `[data-i="0..7"]` 80ms tier (CSS-only stagger fallback).

**JS:** `[data-split-reveal]` handler v0.22.0 copre i nuovi target automaticamente.

### B — `.sl-btn` transition + 8 stati + primary bronze fill ✓ (v0.22.1+v0.22.2)
**v0.22.1 (additivo a v0.20.0 8-state):**
- Base `transition: color, transform var(--dur-fast) quart-out` + `will-change: transform`
- `:hover` (gated `@media hover/no-reduced-motion`) → bronze color + translateY(-1)
- `:active` override → translateY(0) scale(0.98) duration 100ms
- `[aria-busy=true]` → opacity 0.6 pointer-events: none cursor: progress
- `prefers-reduced-motion` → hover transform: none

**v0.22.2 (carry-over Duccio):**
- `.sl-btn--primary` transition extended con `background` prop
- `.sl-btn--primary:hover` → **bronze fill** (`background: var(--accent)`, `color: var(--background)`, `padding: 4px 12px`, `translateY(-1)`)
- Gated `@media (hover: hover) and (prefers-reduced-motion: no-preference)`
- Editorial register no-fill conservato per `.sl-btn` standard (text-only) — solo primary CTA si "accende"

### C — Drop-cap consistency cross-template ✓ (v0.22.1+v0.22.2)
**v0.22.1:** `.sl-competenza__prose > p:first-of-type::first-letter` upright 84px (tier-2).

**v0.22.2 forward-compat:**
| Scope | Stato |
|---|---|
| `.sl-attorney__bio > p:first-of-type::first-letter` | edge case (p direct child if futuro markup) |
| `.sl-attorney__bio-prose > p:first-of-type` | esistente v0.10 (84px italic) |
| `.sl-chi-siamo-w3 .sl-page__prose > p:first-of-type` | forward-compat (template w3 futuro) |
| `.sl-chi-siamo-w3__story > p:first-of-type` | forward-compat |
| `.sl-competenza-w3__body > p:first-of-type` | forward-compat (v0.23.0 saltelli-s2-practice-tier1.jsx namespace) |
| `.sl-practice-tier1 .sl-page__prose > p:first-of-type` | forward-compat (v0.23.0) |
| Mobile compress (≤767) | font-size 60px, margin 4px 12px 0 0 |

**Note skip:**
- `.sl-chi-siamo__prose` (current /chi-siamo/) usa manual `<span class="sl-chi-siamo__dropcap">` — incompatibile con `::first-letter` (duplicate dropcap se aggiunto). Manual span resta source-of-truth.
- Tier-1 esistente `.sl-competenza--tier-1 .sl-competenza__prose` (italic 96px, specificity 0,3,2) wins su tier-2 nuovo (0,2,2).

## Smoke test live (post v0.22.2)

```
URL                                          HTTP   sl-word   note
/                                            200    >2        homepage hero (v0.22.0)
/avvocati/                                   200    2         "Quattro professionisti." ✓
/blog/                                       200    1         "Editoriale." 1-word intrinseco
/tipo-area/privati/                          200    3         term + breadcrumb words
/glossario-legale/                           200    2         "Glossario legale." ✓
/chi-siamo/                                  200    5         "Un atelier di quattro professionisti." ✓
/casi/                                       200    >2        "Casi rappresentativi." (v0.22.0)
/avvocati/emiliano-saltelli/                 200    >2        h1 stagger + bio drop-cap esistente
/competenze/diritto-tributario/              200    >2        h1 stagger + drop-cap tier-1 italic
```

**CSS markers:**
- `sections.css` v0.22.1+v0.22.2: **5 occorrenze**
- `components.css` v0.22.1+v0.22.2: **6 occorrenze**
- Totale: **11 markers** (≥3 atteso) ✓

**`.sl-btn--primary:hover` fill rule deployed:**
```
background: var(--accent);
color: var(--background);
transform: translateY(-1px);
padding: 4px 12px;
```

## Cross-template animation final state

| Layer | Coverage |
|---|---|
| Hero `.sl-word` stagger | **9/9 page** con h1 (homepage + 4 archive/tax/home/glossario + 4 page custom + 4 single) |
| Card hover translate | 7+ rules cross-element |
| Drop-cap | 8/9 page prose-heavy (404 escluso) — homepage `.sl-studio` · single-post · attorney bio · competenza tier-1 italic + tier-2 upright · /chi-siamo/ manual span · forward-compat per v0.23.0 |
| `.sl-btn` 8 stati interattivi | text-only standard + bronze-fill primary CTA |
| `prefers-reduced-motion` | 14+ CSS rules + JS bail-out |
| `@media (hover: hover)` desktop-only | 5+ query (hero CTA, card translate, .sl-btn standard, .sl-btn primary, btn hover gated) |
| IntersectionObserver | 1 instance global (no-GSAP fallback) + ScrollTrigger esistenti |

## Hard rules rispettate

✓ NO modifiche `tokens.css` (riusa `--dur-fast/base/slow`)
✓ NO nuove librerie JS (GSAP + IntersectionObserver esistenti)
✓ Marker `IMPECCABLE v0.22.1` + `v0.22.2` ≥3 (totale 11 occorrenze)
✓ `prefers-reduced-motion` enforced (universale + per-rule)
✓ `@media (hover: hover)` desktop-only su tutti gli hover state
✓ Easing signature: `cubic-bezier(0.25, 1, 0.5, 1)` quart-out
✓ Helper `saltelli_split_h1_words()` riusabile (extra_class param backward-compatible)
✓ Smoke test 9 URL post-deploy

## Deploy chain

```
[v0.22.1 — 751f3df]
   git commit → push origin main
   rsync → /var/www/saltelli/wp-content/themes/saltelli/
   wp cache flush · php-fpm reload

[v0.22.2 — 17a3fbb] (carry-over)
   git commit → push origin main
   rsync → /var/www/saltelli/wp-content/themes/saltelli/
   wp cache flush · php-fpm reload
   Live: 0.22.2-beta-animation-final-completion
```

## Issue residui

- `/blog/` h1 "Editoriale." mono-parola → 1 sl-word totale. Stagger funziona ma effetto visivo limitato. Ipotesi futura: rinominare hero copy in 2-3 parole (es. "Editoriale Saltelli." o "Editoriale del studio.").
- Tier-2 competenze drop-cap (84px upright) vs tier-1 (italic 96px): differenziazione voluta da CLAUDE.md tier split. Da rivedere se cliente nota il salto stilistico.
- `.sl-btn--primary:hover` fill bronze — divergenza dal philosophy editorial no-fill v0.20.0 ma applicato per emphasis CTA primary (decisione Duccio carry-over).
- 5 selettori drop-cap forward-compat oggi senza match DOM. Si attivano automaticamente quando v0.23.0 introduce i namespace `.sl-chi-siamo-w3`, `.sl-competenza-w3__body`, `.sl-practice-tier1`. Cost: ~250 byte CSS dead, accettabile.

## Next

GO Sessione 2 `/costi/` implementation v0.23.0 (JSX `saltelli-s2-costi.jsx` ready, namespace `.sl-costi-w4__`)
GO v0.23.0 tier-1 deep cluster (`.sl-practice-tier1`) + ACF + `.sl-input/.sl-link` polish
o GO v1.0.0 production cut

---
*Tempo effettivo: ~25 min totale (v0.22.1 18 min + v0.22.2 7 min carry-over) · Author: claude-code (Aldo) · Reviewer: Duccio*
