# v0.22.0 Impeccable Animation Consistency

**Version:** `0.22.0-beta-impeccable-animations`
**Branch:** main · **Commit:** `d209edf`
**Deploy:** rsync droplet `saltelli-staging-ams3-01` · cache flush + php-fpm reload
**Date:** 2026-05-02

## Score: 5/5 layers PASS

## Audit pre-fix (5 page campione)

| Pagina | h1 .sl-word | data-reveal | drop-cap | hover unified |
|---|---|---|---|---|
| `/` | ✓ (esistente) | ✓ ScrollTrigger | n/a | parziale |
| `/chi-siamo/` | ✗ → ✓ | ✗ | manual span (no anim) | ✗ |
| `/competenze/diritto-tributario/` | ✗ → ✓ | parziale | tier-1 only | ✗ |
| `/avvocati/emiliano-saltelli/` | ✗ → ✓ | parziale | ✓ statico | ✗ |
| `/casi/` | ✗ → ✓ | parziale | n/a | ✗ |

## Per layer

### L1 — Universal `prefers-reduced-motion` safety net ✓
File: `sections.css` (append v0.22.0). Globale `*, ::before, ::after`:
- `animation-duration: 0.01ms !important`
- `transition-duration: 0.01ms !important`
- `scroll-behavior: auto !important`

### L2 — Page-load orchestration cross-h1 (.sl-word stagger) ✓
**PHP:** `saltelli_split_h1_words($title)` in `inc/helpers.php` — wrappa parole in `<span class="sl-word">`, preserva `<em>` + `<br>` whitelisted.
**Templates patched:**
- `single-avvocato.php` → `.sl-attorney__name`
- `single-competenza.php` → `.sl-competenza__title`
- `single.php` → `.sl-post__title`
- `page.php` → `.sl-page__title`, `.sl-casi__h1`, `.sl-contatti-w3__h1`, `.sl-chi-siamo__h1`

**JS (`main.js`):** loop `[data-split-reveal]` (era selector singolo); GSAP per ogni headline; bail-out `prefers-reduced-motion` aggiornato.
**CSS:** `.sl-word` stato iniziale `opacity: 0; translateY(24px)`; `.is-revealed` rivela. Fallback no-JS (`html:not(.js-reveal-ready)`) → visibile.

Verified live: 7/7 page con h1 .sl-word output corretto.

### L3 — Scroll-driven reveals ✓
- ScrollTrigger esistente (riusato)
- IntersectionObserver fallback aggiunto (no-GSAP path):
  ```
  rootMargin: '0px 0px -10% 0px', threshold: 0.1
  ```
- CSS `[data-reveal-delay="N"]` (1-4) → transition-delay 80/160/240/320ms

### L4 — Micro-interactions card hover unified ✓
**`@media (hover: hover) and (prefers-reduced-motion: no-preference)`** (NO touch device):
- `.sl-blog2__card`, `.sl-attorney__card` → `translateY(-4px)`
- `.sl-cases__row` → `translateX(8px)`
- Transition `var(--dur-base)` + cubic-bezier(0.25, 1, 0.5, 1)

**`.sl-btn--primary` (L6):** subtle `translateY(-1px)` su hover.

`.sl-btn` 8 stati già coperto da v0.20.0 (components.css).

### L5 — Drop-cap consistency ✓
**Static rules esistenti** (riusati):
- `.sl-studio__prose[data-drop-cap]` (homepage)
- `.sl-attorney__bio-prose` (single-avvocato)
- `.sl-competenza--tier-1 .sl-competenza__prose` (tier-1 only)
- `body.single-post .sl-post__body` (single posts)
- `.sl-chi-siamo__dropcap` (manual span, /chi-siamo/)

**Reveal animation aggiunta** per `.sl-page__prose / .sl-competenza__prose` first-letter:
```
opacity 0 → 1 + scale(0.92) → 1, dur-slow, delay 200ms
```
Trigger: `.is-revealed` su parent `<p>` (via JS o IntersectionObserver).

**Note residua:** /chi-siamo/ usa span manuale (`.sl-chi-siamo__dropcap`), separato dal sistema `::first-letter` — intenzionale (template-specific layout).

### L7 — Section-head children stagger ✓
CSS animation `sl-fade-up` (16px translate + opacity). Delay sequenziale:
- `:nth-child(1)` 0ms · `:nth-child(2)` 80ms · `:nth-child(3)` 160ms

Trigger: `html.js-reveal-ready` (auto-applied da main.js DOMContentLoaded).

## Animation budget

| Metric | Value |
|---|---|
| GSAP new uses | 0 (riusa esistenti) |
| ScrollTrigger | invariato |
| IntersectionObserver | 1 instance global (fallback only) |
| CSS transitions/animations new | 7 layer block (~150 LOC append) |
| `prefers-reduced-motion` | enforced universalmente + scoped @media (hover) |
| `will-change` | NON applicato (GPU compositor gestisce transform/opacity) |
| Mobile hover | disabled via `@media (hover: hover)` |
| Touch tap | nessun side-effect |

## Lighthouse impact stimato

- Animation Performance: **invariato** (GPU-accelerated transform/opacity only)
- LCP: **invariato** (animations enrichment post-paint, fallback no-JS visibile)
- CLS: **0** (transform-only, no layout shift)
- Accessibility: **+** (focus-visible già da v0.20.0, reduced-motion universale, hover gated)

## Smoke test 9 URL post-deploy

```
/                                         200 ✓
/chi-siamo/                               200 ✓
/casi/                                    200 ✓
/contatti/                                200 ✓
/blog/                                    200 ✓
/tipo-area/privati/                       200 ✓
/glossario-legale/                        200 ✓
/competenze/diritto-tributario/           200 ✓
/avvocati/emiliano-saltelli/              200 ✓
```

DOM verify: `<h1 ... data-split-reveal>` + `<span class="sl-word">` rendered su /chi-siamo/, /competenze/X/, /avvocati/X/ ✓

## Issue residui

- `archive-avvocato.php` + `archive-competenza.php` `<h1 class="sl-section-title">` non ancora wrappati con helper (low priority — section-head stagger L7 already animates eyebrow + title + lede a livello CSS).
- `.sl-chi-siamo__dropcap` manual span resta statico (template-specific design choice, fuori dal sistema `::first-letter`).
- Tier-2 competenze (`/competenze/diritto-bancario/`, ecc) drop-cap non attivato — il pattern esistente è gated `.sl-competenza--tier-1`. v0.22.0 aggiunge reveal animation ma NON estende drop-cap a tier-2 (richiederebbe decisione design separata).
- Mobile hover transitions disabled via `@media (hover: hover)` — touch users vedono solo state default + active. Acceptable per editorial register.

## Hard rules rispettate

✓ NO modifiche `tokens.css` (riusa --dur-fast/base/slow + --ease-editorial)
✓ NO nuove librerie JS (solo GSAP + ScrollTrigger esistenti + native IO)
✓ `prefers-reduced-motion` enforced UNIVERSALLY
✓ Performance budget: GPU transform/opacity only, no LCP-blocking
✓ Mobile: hover gated `@media (hover: hover)`
✓ Easing signature: `cubic-bezier(0.25, 1, 0.5, 1)` quart-out (no bounce/elastic)

## Next

GO ulteriori micro-fix UX
o GO Step F production cut (WOFF2 ✓ + SRI ✓ + Lighthouse target ≥92 ⏳ verify)
o GO v1.0.0 production cut

---
*Tempo effettivo: ~50 min · Author: claude-code (Aldo) · Reviewer: Duccio*
