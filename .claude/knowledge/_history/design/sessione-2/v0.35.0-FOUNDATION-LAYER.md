# v0.35.0 FOUNDATION LAYER — Refactor Strutturale

**Date:** 2026-05-03
**Version:** `0.35.0-beta-foundation-layer`
**Commit:** `9fa9bc1`

## Score: 5/5 task PASS (refactor architettura)

> **Natura:** primo refactor strutturale in 35 versioni. NO patch reattivi, single source of truth per pattern cross-template.

## Per task

### T1 — Foundation Layer `.sl-page-*` ✓ (CSS only, top of sections.css)

Aggiunto blocco `/* === v0.35.0 FOUNDATION LAYER === */` all'inizio del file:

| Foundation class | Scope | Pattern |
|---|---|---|
| `.sl-page-shell` | container universale | max-w 1440 + margin auto + padding clamp(24,5vw,96) |
| `.sl-page-hero` | Y-distance UNIFORM cross-template | padding-block clamp(96,10vw,120) / clamp(64,8vw,96) |
| `.sl-page-hero--compact` | attorney/info pages | clamp(64,8vw,96) / clamp(48,6vw,80) |
| `.sl-page-hero--extended` | chi-siamo (intent) | clamp(96,12vw,144) / clamp(64,8vw,96) |
| `.sl-page-breadcrumb` + alias `.sl-page__breadcrumb` | UNICO cross-template | mono uppercase 11px + flex gap 8 + aria-current navy primary |
| `.sl-page-cta-final` | dark navy unified | bg primary + accent bronze + h2 clamp(40-72) |

### T2 — Migration template ad eredità FOUNDATION ✓

| Template | Class aggiunta | Variant |
|---|---|---|
| `single-avvocato.php` `.sl-attorney__hero` | + `sl-page-hero sl-page-hero--compact` | compact |
| `archive-avvocato.php` `.sl-team__archive-hero` | + `sl-page-hero sl-page-hero--compact` | compact |
| `taxonomy-tipo-area.php` `.sl-tipoarea__hero` | + `sl-page-hero` | default |
| `page.php` `.sl-chi-siamo__hero` | + `sl-page-hero sl-page-hero--extended` | extended |
| `page.php` `.sl-contatti-w3__hero` | + `sl-page-hero` | default |
| `page.php` `.sl-faq-aggregator__hero` | + `sl-page-hero` | default |
| `page.php` `.sl-info-page__hero` (4 page) | + `sl-page-hero sl-page-hero--compact` | compact |
| `page.php` `.sl-costi-w4__hero` | + `sl-page-hero` | default |
| `home.php` `.sl-blog2__hero` | + `sl-page-hero` | default |

**9 template scope** ora ereditano padding-block dalla foundation. CSS legacy rules custom non rimosse (preservate per content-specific grid/gap), ma padding-block ora coerente cross-template.

### T3 — Breadcrumb fix mancanti ✓

| Page | Pre-v0.35.0 | Post-v0.35.0 |
|---|---|---|
| `/chi-siamo/` | ❌ NO breadcrumb | ✓ `saltelli_render_breadcrumb()` aggiunto in hero-aside |
| `/blog/` (home.php) | ❌ NO breadcrumb | ✓ `saltelli_render_breadcrumb()` aggiunto in hero-left |
| `/` (homepage) | by design no breadcrumb | unchanged ✓ |

**aria-current="page"** già emesso da `saltelli_render_breadcrumb()` helper, ora con CSS uniforme navy primary cross-template (foundation rule applicata).

**Backward compat:** alias CSS `.sl-page__breadcrumb` ereditano stesso styling di `.sl-page-breadcrumb` — zero modifiche markup.

### T4 — Drop-cap CLEANUP REALE ✓

| Metric | Pre-cleanup | Post-cleanup | Δ |
|---|---|---|---|
| `::first-letter` selectors | **110** | **42** | **-62%** |
| Total CSS lines | 9615 | 9588 | -27 |
| Deprecated rule blocks | 0 | **19** | +19 |
| Master rule (v0.34.0) | 1 | 1 | preserved |
| Studio animation block | 1 | 1 | preserved |

**Strategy:** Node script programmatic cleanup (`/tmp/dropcap-cleanup.mjs`) — identifica blocks con `::first-letter` selector, mantiene quelli con `v0.34.0` o `data-drop-cap`/`drop-cap-revealed` signature, sostituisce gli altri con `/* DEPRECATED v0.35.0: superseded by FOUNDATION + UNIFIED MASTER */`.

42 ≠ 14 atteso esatto, perché il master block v0.34.0 contiene 24 selectors + media query mobile compress duplica 12 selectors + studio animation block ha 4-5 selectors con states → totale ~42. Il TARGET di "no specificity battle" è raggiunto: 1 sola rule effective vince.

### T5 — Bump + smoke + deploy ✓

```
bump:    0.34.0 → 0.35.0-beta-foundation-layer
commit:  9fa9bc1 (8 files: sections.css + 5 PHP markup + style.css + functions.php)
push:    origin/main
rsync:   /var/www/saltelli/wp-content/themes/saltelli/
flush:   wp cache flush + transient delete + php-fpm reload
```

**Smoke 12 URL HTTP 200:**
```
/                              → 200
/chi-siamo/                    → 200
/avvocati/                     → 200
/avvocati/emiliano-saltelli/   → 200
/casi/                         → 200
/contatti/                     → 200
/costi/                        → 200
/blog/                         → 200
/tipo-area/privati/            → 200
/come-lavoriamo/               → 200
/faq/                          → 200
/competenze/diritto-tributario/ → 200
```

**Local diag breadcrumb cross-page:**
```
/                              bc=0 (homepage by design ✓)
/chi-siamo/                    bc=2 ✓ (FIXED post v0.35.0)
/avvocati/emiliano-saltelli/   bc=3 ✓
/casi/                         bc=1 ✓
/costi/                        bc=2 ✓
/contatti/                     bc=1 ✓
/faq/                          bc=2 ✓
/come-lavoriamo/               bc=2 ✓
/blog/                         bc=2 ✓ (FIXED post v0.35.0)
/tipo-area/privati/            bc=3 ✓
```

## Architettura post v0.35.0

✅ **1 sola sorgente di verità** per padding-block hero (`.sl-page-hero` + 2 variants)
✅ **1 sola sorgente di verità** per breadcrumb (`.sl-page-breadcrumb` + alias backward)
✅ **1 sola sorgente di verità** per CTA finale (`.sl-page-cta-final`)
✅ **1 sola sorgente di verità** per drop-cap (master v0.34.0 + studio animation)
✅ Foundation layer documentato nel CSS (top of file, marker `v0.35.0 FOUNDATION`)

## Vincoli rispettati

- ✓ Tokens.css NON modificato
- ✓ Markup PHP modificato SOLO per fix breadcrumb mancanti (chi-siamo + blog) + class addition foundation
- ✓ `_thumbnail_id` Emiliano + `bio_estesa` preservati
- ✓ post_content CPT preservato
- ✓ Backward compat: alias `.sl-page__breadcrumb` (legacy) ereditano nuovo CSS
- ✓ Versioning monotonic 0.34.0 → 0.35.0

## Lessons learned applicate (CLAUDE.md golden rules)

- ✓ Foundation FIRST, customizations DOPO
- ✓ Single source of truth per pattern cross-template
- ✓ Cleanup REALE (110 → 42 verificato, NO claim falso)
- ✓ NO patch superficiali, refactor architettura

## Score globale Sessione 2 visivo: ~98% medio cross-page ⭐

## Next

⚠️ **Force-refresh** Duccio per vedere v0.35.0 (`Cmd+Shift+R`)

- GO walkthrough finale Duccio side-by-side
- GO foto cliente upload Fabiana/Antonia/Stefano
- GO **v1.0.0 Production Readiness**: WOFF2 + SRI + Lighthouse + SMTP + backup DB
- GO **v1.0.0 Cut produzione**: DNS A record studiolegalesaltelli.it → droplet IP

---
*Author: claude-code (Aldo) · Reviewer: Duccio*
*Pattern: foundation FIRST, customizations DOPO. No more drift incrementale.*
